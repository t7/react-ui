---
menu: 'Components'
name: "Forms"
route: "/forms"
---

import { Playground, PropsTable } from 'docz'
import {
  Button,
  Checkbox,
  Input,
  Select,
  Textarea,
  Textdiv
} from './'

# forms/button

<PropsTable of={Button} />

# forms/checkbox

<PropsTable of={Checkbox} />

# forms/input

<PropsTable of={Input} />

# forms/select

<PropsTable of={Select} />

# forms/textarea

<PropsTable of={Textarea} />

# forms/textdiv

<PropsTable of={Textdiv} />

## Basic Usage

<Playground>
  <p>
    <Button
      mode='primary'
      size='small'
      children='Button'
    />
  </p>

  <p>
    <Checkbox
      label='Checkbox'
      checked={false}
      handleChange={() => {
        console.log('checked')
      }}
    />
  </p>

  <p>
    <Input
      required // true
      label='Input'
      value='test value'
      handleChange={() => {
        console.log('input changed')
      }}
    />
  </p>

  <p>
    <Select
      required // true
      label='Select'
      options={[
        {
          value: '',
          name: 'Select...'
        },
        {
          value: '1',
          name: 'Uno'
        },
        {
          value: '2',
          name: 'Dos'
        },
        {
          value: '3',
          name: 'Tres'
        }
      ]}
      value='1'
      handleChange={() => {
        console.log('select changed')
      }}
    />
  </p>

  <p>
    <Textarea
      label='Textarea'
      value='test value'
    />
  </p>

  <p>
    <Textdiv
      label='Textdiv'
      value='test value'
    />
  </p>
</Playground>
